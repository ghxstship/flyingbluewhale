node_modules
.next
.vercel
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
playwright-report
test-results
.claude/scheduled_tasks.lock
.claude/worktrees/

# Themes × responsive audit — per-cell screenshots (80MB+). Retained for
# 30 days as CI artifacts via .github/workflows/themes-matrix.yml instead.
# Text evidence (coverage.csv/jsonl, probe JSONs, report.json) stays.
docs/audits/evidence/chromium/
docs/audits/evidence/firefox/
docs/audits/evidence/webkit/
docs/audits/evidence/.report/
test-results/
e2e/.auth/

# Capacitor native projects — generated locally via `npx cap add ios/android`.
# Each project is ~50MB+ of native tooling state; keep out of git.
/ios/
/android/

# SEA TRIAL run artifacts (screenshots, results, saved auth sessions)
docs/audits/evidence/sea-trial/

# Workflow audit run artifact (regenerate via scripts/workflow-audit.mjs)
docs/audits/workflow-audit.json

# vitest coverage output
coverage/
