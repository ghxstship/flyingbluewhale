# scripts/routes.txt — pending-stub queue for generate-stubs.sh.
#
# THE ROUTE TREE (src/app) IS THE TRUTH, NOT THIS FILE. The historical
# full-IA manifest that used to live here went stale (it still listed
# retired routes like /m/wallet after the shipped route became /m/pass)
# and re-running the generator could resurrect retired surfaces as stubs —
# flagged by the kit-29 DS conformance audit (2026-07-17), which now diffs
# against the real tree. docs/ia/SITEMAP.md (npm run gen:sitemap) is the
# generated route-surface SSOT.
#
# To scaffold NEW routes: add lines below in the form
#   <relative_path_under_src/app>|<title>|<description>
# run `bash scripts/generate-stubs.sh` (idempotent — never overwrites),
# then REMOVE the processed lines so this file never accumulates fiction.
