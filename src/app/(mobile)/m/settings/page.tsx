export const metadata = { title: 'Settings' };

export default function SettingsPage() {
  return (
    <div className="p-8">
      <h1 className="text-heading text-2xl text-text-primary mb-2">Settings</h1>
      <p className="text-text-secondary text-sm">Offline sync, camera permissions, and preferences.</p>
    </div>
  );
}
