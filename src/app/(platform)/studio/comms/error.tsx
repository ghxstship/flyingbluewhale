"use client";

// F-08 — module-segment error boundary: a throwing widget here replaces only
// this module body, not the whole console page. Shared body in
// src/components/SegmentError.tsx.
export { SegmentError as default } from "@/components/SegmentError";
