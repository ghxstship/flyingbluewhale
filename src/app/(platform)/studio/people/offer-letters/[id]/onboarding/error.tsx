"use client";

// Kit 30 — packet error boundary (five-state contract: error + retry).
// Shared body in src/components/SegmentError.tsx.
export { SegmentError as default } from "@/components/SegmentError";
