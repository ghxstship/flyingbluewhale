"use client";

// Kit 30 — segment error boundary with retry; a throwing roster query
// replaces only this module body, not the whole console page.
export { SegmentError as default } from "@/components/SegmentError";
