import "./heat.css";
import { HeatProposalView } from "./HeatProposalView";

export const metadata = {
  title: "Miami HEAT × Agora — Pop-Up Activation Program",
  description:
    "Tiered activation buildouts for the East Plaza inside Kaseya Center. Baseline retainer plus Small / Medium / Large per-activation menu.",
};

export default function HeatProposalPage() {
  return <HeatProposalView />;
}
