'use client';

/** Stub: SortableHeader — scaffold for future implementation */
export function SortableHeader({ children, ...props }: any) {
  return <div data-component="SortableHeader" {...props}>{children}</div>;
}
export default SortableHeader;
