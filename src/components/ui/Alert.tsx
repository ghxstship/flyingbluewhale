'use client';

/** Stub: Alert — scaffold for future implementation */
export function Alert({ children, ...props }: any) {
  return <div data-component="Alert" {...props}>{children}</div>;
}
export default Alert;
