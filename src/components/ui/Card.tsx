'use client';

/** Stub: Card — scaffold for future implementation */
export function Card({ children, ...props }: any) {
  return <div data-component="Card" {...props}>{children}</div>;
}
export default Card;
