'use client';

/** Stub: SearchInput — scaffold for future implementation */
export function SearchInput({ children, ...props }: any) {
  return <div data-component="SearchInput" {...props}>{children}</div>;
}
export default SearchInput;
