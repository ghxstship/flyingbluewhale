'use client';

/** Stub: Tabs — scaffold for future implementation */
export function Tabs({ children, ...props }: any) {
  return <div data-component="Tabs" {...props}>{children}</div>;
}
export default Tabs;
