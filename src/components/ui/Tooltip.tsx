'use client';

/** Stub: Tooltip — scaffold for future implementation */
export function Tooltip({ children, ...props }: any) {
  return <div data-component="Tooltip" {...props}>{children}</div>;
}
export default Tooltip;
